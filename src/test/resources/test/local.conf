host {
  kibana = "http://localhost:5601"
  es = "http://localhost:9300"
  version = "8.0.0"
}

security {
  on = true
}

auth {
  providerType = "basic"
  providerName = "basic"
  username = "elastic"
  password = "changeme"
}