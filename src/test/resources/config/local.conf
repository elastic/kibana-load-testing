host {
  kibana = "http://localhost:5620"
  es = "http://localhost:9220"
  version = "8.0.0"
}

security {
  on = true
}

auth {
  providerType = "basic"
  providerName = "basic"
  username = "elastic"
  password = "changeme"
}