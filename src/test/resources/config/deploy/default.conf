elasticsearch {
    deployment_template = "gcp-io-optimized"
    hot_content {
        instance_configuration_id = "gcp.data.highio.1"
        memory = 8192
    }
}

kibana {
    memory = 1024
}

apm {
    memory = 512
}
